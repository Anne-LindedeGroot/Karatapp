#!/bin/bash

# 🚀 FLUTTER-RUN-FAST
# Drop-in replacement for 'flutter run' with ultra-fast optimizations
# Usage: ./scripts/flutter-run-fast [flutter run arguments]

# Get the project root directory (parent of scripts directory)
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# Change to project root
cd "$PROJECT_ROOT"

# Execute the ultra-fast script with all passed arguments
exec ./scripts/ultra_fast_dev.sh "$@"



